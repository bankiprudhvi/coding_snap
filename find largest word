
function findLargeWord(){
  let word="Prudhvi kumarInfo is good boy"
  const arr=word.split(" ")
  let temp=""
  for(let i=0;i< arr.length;i++){
      if(arr[i].length>temp.length){
        temp=arr[i]
      }
  }
  return temp
}
const result=findLargeWord();
console.log(result)
